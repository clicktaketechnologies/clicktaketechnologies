{
  "name": "clicktake-media-failover-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that fronts all media CDN origins with automatic failover + R2 fallback.",
  "main": "media-failover-router.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.95.0"
  }
}
